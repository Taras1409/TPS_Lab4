version: '3.8'

services:

  product-service:
    build: ./product_service
    ports:
      - "8001:8000"

  order-service:
    build: ./order_service
    ports:
      - "9001:8000"
    depends_on:
      - product-service